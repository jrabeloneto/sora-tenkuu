// scenes/stubs.tsx — §01–§05 placeholders.
// Each will become its own lazy-mounted scene per the build order in AESTHETIC_SPEC.md.
// They exist now so the scroll choreography, camera rooms and code-splitting
// boundaries are already wired before the heavy scenes land.

/* §01 — CHROME FORMS (BLUST gallery)
   TODO: cluster of metallic spheres/torus orbiting; magnetic pointer attraction
   (Float + lerp toward cursor); frost-glass captions; iridescent fresnel pulse on hover. */
export function ChromeFormsStub() {
  return null
}

/* §02 — CRYSTAL CITY (signature flythrough)
   TODO: procedural/instanced MeshTransmissionMaterial towers + ribbon roads,
   god-rays, heavy bloom, sparkles. Slow the Lenis velocity through this room. */
export function CrystalCityStub() {
  return null
}

/* §03 — CRT ROOM (webcore interlude)
   TODO: instanced CRT wall on --void, emissive deep-blue screens with blinking
   eye/glitch shader, scanline+grain+CA cranked, bezier cable tubes. */
export function CRTRoomStub() {
  return null
}

/* §04 — ARTIFACT ARCHIVE (Y2K device museum)
   TODO: pinned horizontal scroll, devices on rotating pedestals,
   Nokia-poster editorial captions, frost-glass detail cards on click. */
export function ArtifactArchiveStub() {
  return null
}

/* §05 — OUTRO (dissolve to sky + flare, logotype reprise, frost footer) */
export function OutroStub() {
  return null
}
