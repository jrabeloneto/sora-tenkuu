# SORA / 天空

Y2K Japanese-futurism experiential site. React 19 · Vite · R3F · GSAP · Lenis.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # type-check + production build
```

See `AESTHETIC_SPEC.md` for the design source of truth and remaining build order.
